# Materiali Studenti - Lezione 11

Questa cartella contiene i template per preparare il lab L12.

## File

- `consegna.md` - istruzioni pre-lab.
- `template/backend-rules.md` - mapping della regola `urgencyLabel`.
- `template/payload-cases.md` - casi HTTP validi e invalidi.
- `template/request-trace.md` - trace request/server/persistenza da usare in L12.

## Output

Non devi chiudere una patch autonoma in L11.

Devi arrivare a L12 con:

- mapping della regola;
- payload validi/invalidi;
- trace request -> validation -> rule -> Prisma -> response;
- file candidati da modificare;
- fuori scope chiaro.

Non implementare `urgencyLabel` in L11.
